<!DOCTYPE html>
<html>
  <head>
    <title>PrivateEvents</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <style>
    .success {
      background-color: green;
      font-size: 30px;
      }

    .error {
    background-color: red;
    font-size: 30px;
    }

    h1 {
      color: blue;

      }
  </style>

  <body>
    <%= link_to "HOME", root_path %><br>
    <%= link_to "PROFILE", user_path(current_user.id) %><br>
    <%= link_to "Sign Up", new_user_registration_path unless user_signed_in? %><br>
    <%= link_to "Sign In", new_user_session_path unless user_signed_in? %><br>
    <%= button_to "Sign Out", destroy_user_session_path, method: :delete if user_signed_in? %><br>
    <%= link_to "New Event", new_event_path if user_signed_in? %>
    
    <% flash.each do |key, value| %>
      <div class="flash <%= key %>"><%= value %></div>
    <% end %>

    <%= yield %>
  </body>
</html>
